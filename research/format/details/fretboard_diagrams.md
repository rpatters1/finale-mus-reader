# FretboardDiagram

**Covers:** Where the FretboardDiagram detail record's identity and layout are documented.
**Read when:** Working on fretboard diagrams.
**Confidence:** see the referenced file; the fret records were derived together.

The fret record family — `FretInstrument`, `FretboardGroup`, `FretboardStyle`, and
`FretboardDiagram` — was decoded as one investigation and is documented together in
[`../others/fret_records.md`](../others/fret_records.md), because the diagram record's layout is
only interpretable alongside the style and group records it references.

Experiment history: [`../../investigations/fret_records.md`](../../investigations/fret_records.md).

Implementation: `src/import/details/fretboard_diagrams.cpp`.
