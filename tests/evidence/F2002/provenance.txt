Version 2002a.r1
Mac OS 9.0.4 running in SheepShaver

F2002-norevstem-len96.mus
    sha256 4f3603b58bee6f24a475f895346c98df887a0699cde50bbbeff8d3c7d81b67d6
    2,649 bytes. Changes the normal stem length from 84 to 96 and switches off
    "Display Reverse Stemming". Both option words move and no others:
    `20(65534)` word 4 goes 84 -> 96, and `41(65534)` word 1 goes 26 -> 30, a
    gain of 4. The companion follows with `<stemLength>96</stemLength>` and
    `<noReverseStems/>`.
    This pair confirms the two rows the corpus cannot reach. No surveyed document
    varies the normal stem length from 84, and none has reverse stemming switched
    off, so bit 2 of the packed beam-flags word rested on the framework's word
    alone until this save. It also fixes the boundary from the other side: Finale
    1.0.0 and 3.7.2 spell the same flag in bit 0.

F2002-norevstem-len96.etf
    sha256 0e5c93ce954b7c27eb8bee9f4ce682c6cebaaa9bce0ceb4a9444678e6910847c
    The matching Finale 2002 ETF export, whose `^20(65534)` and `^41(65534)` rows
    confirm both edited words independently of the MUS decoding.

F2002-fileinfo-text.mus
    sha256 1df01c0fc5212f682daed1d17b410422d6f39c9f04fdd961ec98220939186059
F2002-fileinfo-text.etf
    sha256 ca9e4249affa357b680192e00b1ab25ba574c3dc8e03e682adb0f6b82f1bad77
F2002-fileinfo-text.musx
    sha256 14f0b0acec06b84891e693e802603a324c5be42bc0edc70c7ee941f5aead50b2
    An authored score with a real page-text layout, to which the four File Info
    fields were added and labelled. It settles that **the header offsets still hold
    across the DCL epoch**: all four are recovered from them here and confirmed
    field for field by the companion, so File Info moves into the text pool
    somewhere after Finale 2002 rather than at the epoch boundary.

    Its twelve block texts are the incidental value and a wider exercise of the
    pool than a purpose-built fixture gives. They carry `^title`, `^composer`,
    `^copyright` and `^description` inserts, several of them mid-sentence and one
    inside parentheses; a block that changes size and style partway through; and a
    long paragraph with curly quotes and an italic run. All twelve agree with the
    companion apart from the font-command spelling.
